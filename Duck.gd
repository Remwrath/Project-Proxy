extends Thing

func _init(hp = 10, mass = 2, hardness = 1).(hp, mass, hardness): #call parent class (Thing) constructor with these parameters
	pass

# Called when the node enters the scene tree for the first time.
func _ready():
	pass



# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
